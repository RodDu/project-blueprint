name: Blueprint CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  validate-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate registry.json
        run: |
          if [ -f scripts/registry.json ]; then
            python -c "import json; json.load(open('scripts/registry.json'))"
            echo "Registry JSON is valid."
          fi

  test-python:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Check Syntax (Discover & Install)
        run: |
          python -m py_compile scripts/discover.py
          python -m py_compile scripts/install.py

  lint-markdown:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Markdown links and syntax
        uses: DavidAnson/markdownlint-cli2-action@v16
        with:
          globs: "**/*.md"
          config: |
            {
              "MD001": false,
              "MD013": false,
              "MD022": false,
              "MD026": false,
              "MD031": false,
              "MD032": false,
              "MD033": false,
              "MD040": false,
              "MD041": false,
              "MD051": false
            }
